{
  "name": "events-polygon-amoy",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ events-polygon-amoy",
    "create-local": "graph create --node http://localhost:8020/ events-polygon-amoy",
    "remove-local": "graph remove --node http://localhost:8020/ events-polygon-amoy",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 events-polygon-amoy",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.91.1",
    "@graphprotocol/graph-ts": "0.32.0"
  },
  "devDependencies": { "matchstick-as": "0.5.0" }
}
